export enum SocketEvent {
  CHARACTER_JOIN = 'characterJoin',
  CHARACTER_JOINED = 'characterJoined',
  CHARACTER_LEAVE = 'characterLeave',
  CHARACTER_MOVE = 'characterMove',
  CHARACTER_MOVED = 'characterMoved',
  LIST_CHARACTER_JOINED = 'listCharacterJoined',
  NEW_CHARACTER_JOINED = 'newCharacterJoined',
}
